{# navbar from Bootswatch #}

<nav class="navbar navbar-expand-lg navbar-light bg-light">
<div class="container-fluid">
    <a class="navbar-brand" href="{{path('homepage')}}">E-Shop Garden</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor03" aria-controls="navbarColor03" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarColor03">
    <ul class="navbar-nav me-auto">

        <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Catégories (HS)</a>
            <div class="dropdown-menu">
                {#! Problème avec variable category. Route OK #}
                {# {% for c in category %} 
                    <a class="nav-link" href="{{path('product_category', {'slug': c.slug})}}">{{ c.name }}</a>
                {% endfor %} #}



            </div>
        </li>
    </ul>
    <ul class="navbar-nav">
        <li class="nav-item">
            <a href="{{ path('cart_browse')}}" class="btn btn-dark m-0"> <i class="fas fa-shopping-cart"> </i> Le panier : {{cartService.detailItems|length}} <br> ({{ cartService.total }} €) </a>
        </li>
        {% if app.user %}
            <li class="nav-item"><a href="#" class="nav-link">Mes commandes</a></li>
            <li class="nav-item"><a href="{{path('app_logout')}}" class="btn btn-outline-warning m-1">Déconnexion</a></li>
        {% else %}
            <li class="nav-item"><a href="#" class="btn btn-outline-info m-1">Inscription</a></li>
            <li class="nav-item"><a href="{{path('app_login')}}" class="btn btn-outline-success m-1">Connexion</a></li>
        {% endif %}

    </ul>
</div>
</div>
</nav>