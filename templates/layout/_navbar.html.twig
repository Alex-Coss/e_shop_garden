{# navbar from Bootswatch #}

<nav class="navbar navbar-expand-lg navbar-light bg-light">
<div class="container-fluid">
    <a class="navbar-brand" href="{{path('homepage')}}">E-Shop</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor03" aria-controls="navbarColor03" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarColor03">
    <ul class="navbar-nav me-auto">

        <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Catégories</a>
            <div class="dropdown-menu">
                {# {% for c in category %}
                    <a class="nav-link" href="{{path('category_slug': c.slug, 'slug': c.slug)}}">{{ c.name }}</a>
                {% endfor %} #}



            </div>
        </li>
    </ul>
    <ul class="navbar-nav">
        <li class="nav-item"><a href="#" class="btn btn-info ms-2">Inscription</a></li>
        <li class="nav-item"><a href="{{path('app_login')}}" class="btn btn-success ms-2">Connexion</a></li>
        <li class="nav-item"><a href="{{path('app_logout')}}" class="btn btn-warning ms-2">Déconnexion</a></li>
    </ul>
    </div>
</div>
</nav>